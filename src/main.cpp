#include <iostream>
#include "GameBoard.h"

int main() {
    GameBoard board(9);
    // Game logic
    // Runs the simulation and records the scores of the players for the players separated with commas
    return 0;
}
